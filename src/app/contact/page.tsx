import type { NextPage } from 'next'

import { Main } from './components/Main'

const Contact: NextPage = () => <Main />

export default Contact

export const metadata = {
	title: 'Contact',
	description: 'Contact page',
}
